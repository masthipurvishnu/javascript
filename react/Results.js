import React from "react";

const Results = ({ player }) => {
    return (
        <div>
            <h1>Results</h1>
            <label>{player}</label>
        </div>
    );
};

export default Results;
